# دليل نظام الاشتراكات (CliQ) — منصة سفرة

تمت إضافة نظام اشتراكات كامل لأصحاب المطاعم، يعتمد على تحويل CliQ يدوي + رفع
سكرين شوت + مراجعة من الأدمن + تفعيل (يدوي مباشر أو عبر كود تفعيل).

رقم CliQ المستخدم في كل الصفحات: **0787000190**

---

## 1) الملفات الجديدة / المعدّلة

| الملف | التغيير |
|---|---|
| `اشتراك.html` | **جديد** — صفحة صاحب المطعم: عرض الخطط، رقم CliQ، رفع سكرين شوت، حالة "قيد المراجعة"، وإدخال كود التفعيل |
| `لوحة_المطعم.html` | شريط حالة الاشتراك أعلى الصفحة + رابط "اشتراكي" بالهيدر |
| `لوحة_الإدارة.html` | تبويب جديد "الاشتراكات": مراجعة الطلبات، تفعيل مباشر، توليد كود تفعيل، رفض، إنهاء اشتراك، وإدارة خطط الاشتراك |
| `firestore.rules` | صلاحيات مجموعتي `subscriptions` و `subscriptionPlans` |
| `firestore.indexes.json` | فهرس مركّب لازم لاستعلام "آخر طلب اشتراك" |
| `functions/index.js` | دالة `onSubscriptionStatusChangedNotifyOwner` — إشعار Push تلقائي لصاحب المطعم عند تغيّر حالة اشتراكه |

---

## 2) كيف تشتغل الدورة (Flow) بالكامل

1. صاحب المطعم يفتح **اشتراك.html** (رابط "اشتراكي" من لوحة تحكمه).
2. يختار خطة اشتراك (المدة والسعر تحددهم إنت من لوحة الإدارة).
3. يحوّل المبلغ عبر CliQ على الرقم **0787000190**.
4. يرفع سكرين شوت التحويل → ينشئ طلب بحالة **"قيد المراجعة"**.
5. إنت (الأدمن) تفتح تبويب **"الاشتراكات"** بلوحة الإدارة، تشوف السكرين شوت،
   وعندك خياران:
   - **✅ تفعيل مباشر (يدوي فوري)**: يفعّل الاشتراك حالاً بدون أي خطوة إضافية
     من صاحب المطعم.
   - **🔑 توليد كود تفعيل**: يولّد كود عشوائي من 6 أرقام، إنت ترسله يدويًا
     لصاحب المطعم (اتصال/واتساب)، وهو يدخله بصفحة `اشتراك.html` ليتفعّل
     حسابه **تلقائيًا** فور إدخال الكود الصحيح (فيه تحقّق حقيقي بقواعد
     الأمان Firestore، مو بس بالواجهة).
   - أو **❌ رفض** الطلب مع كتابة سبب اختياري.
6. لما ينتهي تاريخ الاشتراك (`endDate`)، النظام يكتشف هذا تلقائيًا أول ما
   صاحب المطعم يفتح لوحته أو صفحة الاشتراك، ويحوّل الحالة لـ **"منتهي"**
   (بدون الحاجة لأي Cloud Function مجدولة).
7. صاحب المطعم يقدر يجدد اشتراكه بنفس الخطوات بأي وقت.

---

## 3) خطوات النشر (Deploy) المطلوبة منك

```bash
# 1) قواعد وفهارس Firestore
firebase deploy --only firestore:rules,firestore:indexes

# 2) الاستضافة (كل ملفات HTML الجديدة/المعدّلة)
firebase deploy --only hosting

# 3) دوال الإشعارات (تحتاج خطة Blaze على مشروع Firebase)
firebase deploy --only functions
```

> إذا مشروعك على خطة Spark (المجانية) مش رح تقدر تنشر Functions. باقي
> النظام (الاشتراك، الرفع، المراجعة، التفعيل بالكود) بيشتغل 100% حتى بدون
> نشر functions — الإشعار التلقائي بس هو اللي محتاج Blaze.

---

## 4) قاعدة أمان مطلوبة يدويًا على Firebase Storage

المشروع الحالي ما فيه ملف `storage.rules` مرفق (يبدو إن قواعد Storage
مضبوطة مباشرة من الكونسول). **افتح Firebase Console → Storage → Rules**
وأضف قاعدة تسمح برفع سكرين شوتات الاشتراك تحت مسار `subscription_proofs/`،
مثلاً:

```
match /subscription_proofs/{uid}/{fileName} {
  allow read: if request.auth != null &&
                (request.auth.uid == uid ||
                 request.auth.token.email == "ahmedkhalil7749000@gmail.com");
  allow write: if request.auth != null && request.auth.uid == uid
                && request.resource.size < 5 * 1024 * 1024
                && request.resource.contentType.matches('image/.*');
}
```

ضيفها فوق باقي قواعدك الحالية (متل مسارات `restaurants/` و `menuItems/`)
من غير ما تلمسهم.

---

## 5) أول خطوة بعد النشر: أضف خطة اشتراك واحدة على الأقل

لسا ما في أي خطط اشتراك محفوظة (السعر والمدة إلك تحددهم). روح للوحة
الإدارة → تبويب **الاشتراكات** → قسم **"إدارة خطط الاشتراك"** وأضف أول خطة
(مثلاً: "الاشتراك الشهري"، 15 د.أ، 30 يوم). بدون خطة واحدة فعّالة على
الأقل، صفحة `اشتراك.html` رح تظهر لأصحاب المطاعم رسالة "لا توجد خطط متاحة".

---

## 6) تحديث: صلّحنا مشكلة "شريط الاشتراك ما بيظهر"

كانت الاستعلامات بـ `اشتراك.html` و `لوحة_المطعم.html` تستخدم `orderBy` مع
`where` على حقل مختلف، وهذا بيحتاج **فهرس مركّب (composite index)** ما كان
منشور. لو ما نشرته، Firestore يرمي خطأ صامت والشريط يضل فاضي بدون أي رسالة
للمستخدم.

**الحل:** عدّلنا الاستعلامين ليجيبوا كل السجلات ويرتبوها يدويًا بالجافاسكربت
بدل الاعتماد على `orderBy` — هيك الميزة تشتغل فورًا من غير ما تحتاج تنشر أي
فهرس إضافي. (فهرس `firestore.indexes.json` ضل موجود بالملف احتياطيًا، بس
مش شرط تنشره حتى يشتغل الموقع).

بعد ما تحمّل الملفات الجديدة، تأكد إنك عملت:
```bash
firebase deploy --only hosting,firestore:rules
```
وبعدها جرّب تسجّل مطعم جديد أو افتح `لوحة_المطعم.html` بحساب عنده مطعم —
المفروض الشريط يبين فورًا (رسالة صفراء "اشترك الآن").

## 7) ملاحظة أمان (بنفس روح ملاحظة `تفعيل_صلاحية_الأدمن.html` الموجودة أصلاً)

التحقق من كود التفعيل يتم فعليًا داخل `firestore.rules` (مو بس بالواجهة)،
لكن لأن صاحب المطعم له صلاحية قراءة وثيقة اشتراكه (بما فيها حقل
`activationCode`)، شخص متمرس تقنيًا نظريًا يقدر يقرأ الكود من قاعدة
البيانات مباشرة بدل ما ينتظر يوصله منك. المخاطرة هون محدودة لأن أصلاً ما
حدا يوصل لهاي المرحلة إلا بعد ما إنت راجعت السكرين شوت ووافقت — يعني زر
"تفعيل مباشر" يبقى الخيار الأضمن 100% إذا بدك تتجنب هالنقطة نهائيًا.
