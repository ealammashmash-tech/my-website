# مشروع سفرة — إيش تم إصلاحه وإزاي تشغّل المشروع

## 1. المشكلة الأساسية اللي كانت مانعتك تربط Firebase

ملف `Auth.html` كان يستورد مكتبات Firebase من روابط غلط:

```
❌ .../firebasejs/10.8.0/firebaseApp.js
❌ .../firebasejs/10.8.0/firebaseAuth.js
```

الروابط الصحيحة لازم يكون فيها شرطة (hyphen):

```
✅ .../firebasejs/10.8.0/firebase-app.js
✅ .../firebasejs/10.8.0/firebase-auth.js
```

بدون الشرطة، الرابط يرجع 404 والمتصفح يوقف عن تحميل الصفحة، فـ Firebase ما كان يشتغل أصلاً.

## 2. شو ضفت/عدّلت

| الملف | شو فيه |
|---|---|
| `firebase-config.js` | ملف واحد فيه إعدادات Firebase + تصدير `auth` و `db` و `storage`. كل صفحة تستورد منه بدل ما تكرر نفس الكود. |
| `Auth.html` | معدّل ليستورد من `firebase-config.js`، وضفت زر "استعادة كلمة المرور" يشتغل فعليًا. |
| `المطاعم.html` | صار يجيب قائمة المطاعم *فعليًا* من مجموعة `restaurants` بـ Firestore ويعرضها كبطاقات. |
| `صفحة_المطعم.html` | يجيب تفاصيل مطعم واحد + المنيو تبعه (subcollection) من Firestore، وفيه زر "أضف للسلة" يخزن بـ localStorage. |
| `seed_data.html` | صفحة تستخدمها **مرة وحدة بس** — بتضيف 3 مطاعم تجريبية مع منيو لكل وحدة، عشان تختبر الموقع بدون ما تدخل بيانات يدوي من الكونسول. |
| `الصفحة_الريسيه.html` | صلحت كل الروابط لتطابق أسماء الملفات الحقيقية (كانت الروابط قبل كذا بأسماء ملفات مو موجودة). |
| `firestore.rules` | قواعد أمان مقترحة — انسخها والصقها بـ Firebase Console. |

## 3. خطوات التشغيل

### أ) لازم سيرفر محلي، مش فتح الملف مباشرة

ملفات `type="module"` (زي `import { auth } from "./firebase-config.js"`) ما تشتغل إذا فتحت الملف مباشرة بالمتصفح (`file://...`) بسبب قيود CORS. لازم تشغّلها عن طريق سيرفر محلي بسيط، مثلاً:

```bash
# داخل مجلد المشروع
python3 -m http.server 8000
```

بعدها افتح: `http://localhost:8000/الصفحة_الريسيه.html`

أو استخدم إضافة **Live Server** بـ VS Code.

### ب) فعّل Firestore بالمشروع (إذا لسا ما فعلته)

من Firebase Console → Build → Firestore Database → Create database → اختر test mode مؤقتًا.

### ج) عبّي بيانات تجريبية

افتح `seed_data.html` واضغط الزر مرة وحدة. بعدها روح `المطاعم.html` وشوف المطاعم ظاهرة.

### د) طبّق قواعد الأمان

من Firebase Console → Firestore Database → Rules، انسخ محتوى `firestore.rules` والصقه، ثم اضغط Publish.

## 4. ملاحظة عن الصفحات الباقية

الملفات الثانية اللي رفعتها (`السلة.html`, `إتمام_الطلب.html`, `حسابي.html`, `طلباتي.html`, `لوحة_المطعم.html`, `لوحة_الإدارة.html`, وملفات "Firebase_..." الأربعة) هي حاليًا **تصاميم هيكلية (wireframes)** بس — عناوين ووسوم مكانية بدون كود فعلي متصل بـ Firebase. أنا جهزت لك النمط (pattern) اللي تقدر تطبقه عليهم بنفس طريقة `المطاعم.html` و `صفحة_المطعم.html`:

1. استورد `db` أو `auth` من `firebase-config.js`
2. اجلب البيانات بـ `getDocs`/`getDoc`
3. اعرضها بـ `innerHTML` أو DOM

إذا حابب، أقدر أبني أي وحدة منهم بنفس الطريقة (مثلاً السلة تجيب من localStorage، أو إتمام الطلب يكتب طلب جديد بـ Firestore).
