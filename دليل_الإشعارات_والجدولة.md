# دليل تفعيل نظام الإشعارات الذكية (Push) + الجدولة الزمنية (Cron)

هالميزة الجديدة أضافت طبقة **خادم (Backend)** فوق ميزة الطلب المسبق يلي كانت
موجودة، بحيث التنبيه ما عاد يعتمد بس على المتصفح المفتوح، وصار في:

1. **إشعارات Push حقيقية** توصل حتى لو المستخدم مسكّر المتصفح (Firebase Cloud Messaging).
2. **جدولة زمنية (Cron)** تشتغل على خوادم Firebase كل دقيقة، تفحص الطلبات
   المسبقة وتنبّه الزبون وصاحب المطعم تلقائياً وتحدّث حالة الطلب.
3. إشعار فوري لصاحب المطعم عند وصول أي طلب جديد، وإشعار للزبون عند تغيّر حالة طلبه.

## الملفات الجديدة

| الملف | الوظيفة |
|---|---|
| `functions/index.js` | كود Cloud Functions (الإشعارات + الـ Cron) |
| `functions/package.json` | اعتماديات الدوال (`firebase-admin`, `firebase-functions`) |
| `firebase.json` / `.firebaserc` | إعدادات مشروع Firebase CLI للنشر |
| `firestore.indexes.json` | الفهرس (index) اللازم لاستعلام الـ Cron |
| `push-notifications.js` | كود الواجهة اللي يسجّل توكن الإشعار للمستخدم |
| `firebase-messaging-sw.js` | Service Worker لاستقبال الإشعارات بالخلفية |

## خطوات التفعيل (لازم تنعمل مرة وحدة)

### 1. ترقية المشروع لخطة Blaze
Cloud Functions والجدولة الزمنية (Cloud Scheduler) بتحتاج خطة **Blaze**
(دفع حسب الاستخدام). فيه باقة مجانية سخية شهرياً كافية لمشروع متوسط، بس
لازم تفعّلها من: Firebase Console → إعدادات المشروع → Usage and billing.

### 2. تفعيل Cloud Messaging وتوليد مفتاح VAPID
- Firebase Console → إعدادات المشروع (⚙️) → تبويب **Cloud Messaging**.
- تحت **Web configuration** اضغط **Generate key pair**.
- انسخ القيمة والصقها مكان `"ضع_مفتاح_VAPID_من_إعدادات_فايربيس_هون"`
  داخل ملف `push-notifications.js`.

### 3. تثبيت أدوات Firebase CLI (لو ما هي مثبتة)
```bash
npm install -g firebase-tools
firebase login
```

### 4. تثبيت اعتماديات الدوال
```bash
cd functions
npm install
cd ..
```

### 5. النشر
```bash
firebase deploy --only functions,firestore:rules,firestore:indexes
```
- أول نشر لدالة `checkScheduledOrders` بيفعّل تلقائياً Cloud Scheduler API.
- لو طلع خطأ متعلق بفهرس Firestore، فايربيس بيعطيك رابط جاهز لإنشائه
  (أو استنى ريثما ينتشر `firestore.indexes.json` تلقائياً).

### 6. رفع باقي ملفات الموقع (Hosting) - اختياري
لو بدك تستضيف الموقع نفسه عبر Firebase Hosting:
```bash
firebase deploy --only hosting
```

## كيف تشتغل الميزة بعد النشر

- أول ما يسجّل أي مستخدم (زبون أو صاحب مطعم) دخول، المتصفح بيطلب إذن
  الإشعارات تلقائياً، ولو وافق بيتخزّن توكن الجهاز بوثيقته `users/{uid}`.
- عند إنشاء طلب جديد: صاحب المطعم ياخد إشعار فوري (`onNewOrderNotifyRestaurant`).
- عند تغيّر حالة الطلب: الزبون ياخد إشعار (`onOrderStatusChangedNotifyCustomer`).
- كل دقيقة: دالة `checkScheduledOrders` تفحص الطلبات المسبقة يلي حان
  موعدها، وترسل إشعار للزبون وصاحب المطعم، وتحدّث حالة الطلب لـ
  "قيد المعالجة" تلقائياً.
- التنبيه المرئي داخل الصفحة (البانر + الصوت) يلي كان موجود بالواجهة
  (`orders.html` و `adman.html`) لسا شغّال كطبقة إضافية فورية، حتى لو
  الإشعار الفعلي وصل عن طريق الخادم.

## ملاحظات مهمة

- لازم يكون ملف `firebase-messaging-sw.js` بجذر الموقع بالضبط (نفس مستوى
  `index.html`) عشان يشتغل صح - هذا شرط من متصفحات الويب نفسها.
- الإشعارات ما بتشتغل على `http://` عادي (إلا `localhost` وقت التجربة)؛
  لازم الموقع يكون على `https://` عند النشر الفعلي.
- لو صاحب مطعم أو زبون رفض إذن الإشعارات، رح يستمر يشوف التنبيه المرئي
  جوا الموقع بس لما تكون الصفحة مفتوحة (زي قبل)، بدون إشعار Push خارجي.
